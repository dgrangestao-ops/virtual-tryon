{
  "name": "virtual-tryon",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prebuild": "npm run validate:catalog && node scripts/generate-products.mjs",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "validate:assets": "node scripts/validate-assets.mjs catalog/fremi.json",
    "validate:all": "npm run validate:catalog && npm run validate:assets && npm run validate:pose && npm run test:temples && npm run build",
    "validate:pose": "node scripts/validate-pose-contract.mjs",
    "test:temples": "node scripts/test-temple-solver.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.22",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "sharp": "^0.34.4"
  }
}
